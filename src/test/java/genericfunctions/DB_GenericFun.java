package genericfunctions;

public class DB_GenericFun {
	// Connect + CRUD operation in DB 
	// https://docs.microsoft.com/en-us/azure/cosmos-db/sql-api-java-application
	
/*	    private static final String HOST = "https://docdb-java-sample.documents.azure.com:443/";
	    private static final String MASTER_KEY = "[YOUR_KEY_HERE]";

	    private static CosmosClient cosmosClient = new CosmosClientBuilder()
	            .setEndpoint(HOST)
	            .setKey(MASTER_KEY)
	            .setConnectionPolicy(ConnectionPolicy.getDefaultPolicy())
	            .setConsistencyLevel(ConsistencyLevel.EVENTUAL)
	            .buildClient();

	    public static CosmosClient getCosmosClient() {
	        return cosmosClient;
	    }
*/

}
